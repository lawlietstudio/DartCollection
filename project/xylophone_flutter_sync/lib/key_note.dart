import 'package:flutter/widgets.dart';

class KeyNote
{
  int keyNumber;
  Color keyColor;

  KeyNote(int keyNumber, Color keyColor)
  {
    this.keyNumber = keyNumber;
    this.keyColor = keyColor;
  }
}