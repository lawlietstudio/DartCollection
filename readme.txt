 // use flutter command for current session
 export PATH="$PATH:`pwd`/flutter/bin"

 // show hidden file
 ls -a

 // show hidden file in finder
 shift + command + .

 // update the path
 // https://docs.flutter.dev/get-started/install/macos#update-your-path

// create a file in home
 touch .zshrc

 // enter following, export PATH="$PATH:[PATH_OF_FLUTTER_GIT_DIRECTORY]/bin"
 export PATH="$PATH:/Users/mark/development/flutter/bin"

 // execute this line to "run and save" the .zshrc
 source .zshrc

 // create and start
 flutter create 
 flutter run

 // vs code command 
 command shift P // switch device
 run on top to run
 ctrl space // class option

 // meterial icon theme, awsome flutter snuppets, flutter

 // vs shortcut
    command J // show termina;
    command b // show sidebar
    fn ctrl space // class option
